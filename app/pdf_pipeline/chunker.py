# TODO: Split text into semantic chunks


def chunk_text(text: str, chunk_size: int = 900, overlap: int = 150) -> list[str]:
    """
    TODO: Split text into overlapping chunks
    Aim for semantic boundaries (sections, paragraphs) if possible
    """
    # TODO: Implementation
    return []
